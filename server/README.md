# Server package

Express + TypeScript backend foundation for SubTrack migration.

## Scripts

- `npm run dev -w server`
- `npm run build -w server`
- `npm run typecheck -w server`

## Env

Copy `.env.example` and set at least:

- `DB_DATABASE_URL`
- `JWT_ACCESS_SECRET`
- `JWT_REFRESH_SECRET`
