# Client package

React + Vite + TanStack Router/Query/Form + Zustand foundation for SubTrack migration.

## Scripts

- `npm run dev -w client`
- `npm run build -w client`
- `npm run typecheck -w client`

## Env

Optional:

- `VITE_API_BASE_URL` (default: `/api/v1`)
